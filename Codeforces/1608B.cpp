#include <bits/stdc++.h>

using namespace std;

const int N = 2e5 + 5;

int n, a, b;

void solve() {
    scanf("%d %d %d", &n, &a, &b);
    vector<int> ans;
    ans.emplace_back(0);
    for(int i = 2; i <= n; i++) {

    } 
}

int T;

int main() {
    scanf("%d", &T);
    while(T--) solve();

    return 0;
}